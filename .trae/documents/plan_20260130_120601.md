I will simplify the PM state detection logic in App.tsx to prevent inconsistent states and race conditions.

**1. Replace Ref-Based State with Derived State**
I will replace the complex ref-based state tracking (`pmStopRequestedRef`, `pmRunOnceRequestedRef`, `lastPmRunningRef`, `lastPmModeRef`) with a simpler derived state approach using `useMemo` and `useEffect`.

**2. Consolidate PM Status Logic**
I will create a single `useEffect` that handles PM status changes and user-initiated actions without relying on refs for tracking previous states. This will eliminate race conditions where refs might not be updated in time.

**3. Simplify Error Handling**
I will consolidate the PM error handling logic to use the unified Toast system consistently, removing the mixed approach of both AlertDialogs and Toasts.

**4. Clean up Redundant Refs**
I will remove the unused refs and simplify the state management to be more predictable and React-idiomatic.

**5. Verification**
The simplified logic will be tested to ensure:

* PM status changes are handled consistently

* User actions (start/stop) work correctly without race conditions

* Error states are properly managed

* The AGENTS.md workflow continues to function correctly

