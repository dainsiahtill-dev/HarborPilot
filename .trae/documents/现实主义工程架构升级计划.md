I will implement the **Memory Hygiene** improvements by structuring the memory snapshot and binding evidence references.

### 1. Refactor `loop-director.py`
- Move `summarize_tool_outputs` call **before** `update_memory_snapshot` to make evidence available.
- Pass `evidence_refs` to `update_memory_snapshot`.

### 2. Update `director_memory.py`
- Modify `update_memory_snapshot` to accept `evidence_refs`.
- Structure the `memory_data` to be explicit about layers:
  - `type`: "working" (explicit tag)
  - `working_memory`: Container for summary/next_step.
  - `evidence_refs`: Store the bound evidence (file/line ranges).

### 3. Update `io_utils.py`
- Update `get_memory_summary` to read from the new `working_memory` structure while maintaining backward compatibility.
- Format the output to clearly label "Working Memory".

This approach introduces the requested "Hygiene" (Layering + Evidence) with minimal code changes and no new infrastructure.